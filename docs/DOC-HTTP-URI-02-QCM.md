# QCM : Structure des URI et URL

**1. Quelle partie de l'URL indique le protocole utilisé ?**

* [ ] A) Le domaine (Host)
* [ ] B) Le schéma (Scheme) *
* [ ] C) Le chemin (Path)
* [ ] D) L'ancre (Fragment)

**2. Quelle partie de l'URL n'est JAMAIS transmise au serveur ?**

* [ ] A) Le chemin (Path)
* [ ] B) Les paramètres (Query String)
* [ ] C) L'ancre (Fragment) *
* [ ] D) Le domaine (Host)

**3. Dans `https://mon-site.fr:8080/blog`, que représente `:8080` ?**

* [ ] A) Le domaine
* [ ] B) Le port *
* [ ] C) Le chemin
* [ ] D) Un paramètre

**4. Quel caractère introduit les paramètres (Query String) ?**

* [ ] A) `#`
* [ ] B) `?` *
* [ ] C) `&`
* [ ] D) `/`

**5. Comment sépare-t-on deux paramètres dans une URL ?**

* [ ] A) `;`
* [ ] B) `?`
* [ ] C) `&` *
* [ ] D) `/`

**6. Quelle est la différence entre URI et URL ?**

* [ ] A) L'URL est une URI qui donne l'adresse de localisation. *
* [ ] B) L'URI est sécurisée (HTTPS) et l'URL non.
* [ ] C) L'URI contient des ancres, pas l'URL.
* [ ] D) Il n'y a aucune différence technique.

**7. A quoi correspond `/recherche/resultat` dans un URI ou URL ?**

* [ ] A) Le fragment
* [ ] B) La requête (Query)
* [ ] C) Le chemin (Path) *
* [ ] D) Le schéma

**8. Si vous visitez `page.html#section2`, que reçoit le serveur ?**

* [ ] A) `page.html#section2`
* [ ] B) `page.html` *
* [ ] C) Rien du tout
* [ ] D) `page.html` et `section2` séparément

**10. Quel est le port par défaut pour une connexion HTTPS ?**

* [ ] A) 80
* [ ] B) 443
* [ ] C) 21
* [ ] D) 3000

**11. Si l'utilisateur tape `https://google.com/search?q=php#resultats`, quelle partie exacte le serveur va-t-il recevoir pour décider quoi afficher ?**

**Votre réponse**: 
```


```
